# ArjunaCRM UI

This library was generated with [Nx](https://nx.dev).

## Building

Run `npx nx build arjuna-ui` to build the library.

## Storybook Server

Run `npx nx start arjuna-ui` to start the storybook development server on `localhost:6006`.

## Running unit tests

Run `npx nx test arjuna-ui` to execute the unit tests via [Jest](https://jestjs.io).
